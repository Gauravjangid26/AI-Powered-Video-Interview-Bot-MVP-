Got it — you want a one-day roadmap to build the MVP of this AI-Powered Video Interview Bot using Streamlit and mostly open-source tools.

Here’s how we can split the work into phases with time blocks so you can ship it in one day:

🗺 One-Day Roadmap (8–10 hours)
Hour 0 – 0.5: Setup & Boilerplate
Install Streamlit, OpenAI Whisper (or faster open-source ASR), transformers for LLM, pydub for audio handling.

Create app.py with a basic Streamlit page layout:

Left: Interview UI for candidate.

Right: Report UI for recruiter.

Setup a role description input form.

Hour 0.5 – 2: LLM Integration (Open-Source)
Use Hugging Face models for:

Intro & question generation → tiiuae/falcon-7b-instruct (light, runs via transformers + text-generation-inference or API).

Summary & evaluation → same model or mistralai/Mistral-7B-Instruct.

Build prompt templates:

Intro Prompt: “You are an interviewer for the role: {role}. Write a short greeting…”

Question Prompt: “Generate 5–7 interview questions for the role: {role}…”

Evaluation Prompt: “Analyze the following responses…”

Mock the role description as JSON for quick testing.

Hour 2 – 4: Video Recording in Streamlit
Use streamlit-webrtc for browser-based video recording.

Store recorded videos in temp/ folder as .webm or .mp4.

Allow candidate to review & re-record before submitting.

Hour 4 – 5: Speech-to-Text (Open Source)
Use openai/whisper or faster-whisper for transcription (runs locally).

After video submission:

Extract audio with ffmpeg (via pydub or subprocess).

Transcribe to text.

Store both video + transcription in a responses.json.

Hour 5 – 6: AI-Based Summary & Skill Evaluation
Feed transcriptions + role description into LLM.

Output structured JSON:

json
Copy
Edit
{
  "summary": "...",
  "strengths": ["...", "..."],
  "weaknesses": ["...", "..."],
  "skill_rating": {
    "Technical": 8,
    "Communication": 7,
    "Problem Solving": 6
  }
}
Render results in Streamlit recruiter view.

Hour 6 – 7: UI Enhancements
Candidate view:

Progress bar for questions (1 of 7).

Next button after each video.

Recruiter view:

Summary + ratings in cards.

Option to download report as PDF (fpdf or reportlab).

Hour 7 – 8: Testing & Polishing
Test with 1–2 mock role descriptions.

Verify:

Questions are role-specific.

Video saves properly.

Transcription works.

Summary & ratings are meaningful.

Add error handling for mic/camera permissions.

Final README with:

Tech stack.

Setup commands.

How to run (streamlit run app.py).

Tech Stack Recap
✅ Frontend/UI: Streamlit + streamlit-webrtc (video recording)
✅ LLM: Hugging Face Falcon/Mistral (role intro, questions, summaries)
✅ Speech-to-Text: faster-whisper (local transcription)
✅ Audio/Video Processing: ffmpeg + pydub
✅ Data Storage: JSON files (MVP)
✅ PDF Report: fpdf

